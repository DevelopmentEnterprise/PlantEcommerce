package com.WebMall.repository;

import com.WebMall.model.GoodImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GoodImageRepository extends JpaRepository<GoodImage, Long> {
}
